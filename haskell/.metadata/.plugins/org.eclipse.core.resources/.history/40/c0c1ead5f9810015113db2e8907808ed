module WordCount (wordCount) where

import Data.Char (isDigit, isLetter, toLower)
import Data.Map (Map, empty, insertWith')

wordCount :: String -> Map String Int
wordCount text =
  foldl countWord empty theWords
  where
    theWords = words normalizedText
    countWord m w = insertWith' (+) w 1 m
    normalizedText = map normalizeChar text
    normalizeChar char =
      if isWordChar char then toLower char
      else ' '
    isWordChar char = isDigit char || isLetter char
